import 'package:flutter/material.dart';

class Refersh extends StatelessWidget {
  const Refersh({Key? key}) : super(key: key);
  static const routeName = '/drw';
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white54,
    );
  }
}
